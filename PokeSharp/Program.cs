using System;
using System.Drawing;
using PokeSharp.Types;
using Utility;

namespace PokeSharp
{
    class Program
    {
        public static void Main()
        {
            Console.WriteLine(new Fraction(2, 4) + new Fraction(2, 4));
            Console.ReadKey();
        }
    }
}